# Preferencias locais de maquina: caminhos de scratchpad, permissoes daquele PC.
# Por convencao settings.local.json e pessoal; settings.json seria o compartilhado.
.claude/settings.local.json

# Gerado por ferramentas/mock_historico.py. Regenerar com:
#   python ferramentas/mock_historico.py
# Fica fora do repo porque sao 220KB de fonte em base64 embutida, e o gerador
# (que e a fonte da verdade) esta versionado.
ferramentas/mock_historico.html

# Saidas temporarias de harness/preview
ferramentas/preview.html

# handoff.md e copia publicavel do handoff de maior versao (gerada, nao editar).
# A fonte da verdade e docs/handoffs/handoff_migracao_pitwall_vNN.md
ferramentas/handoff.md
ferramentas/mock_captacao.html

# Bytecode Python das ferramentas
ferramentas/__pycache__/

# Fotos geradas por ferramentas/foto_fila.py. Ficam fora do repo por dois
# motivos: sao saida gerada (o gerador esta versionado, ele e a fonte), e o
# stub do harness usa uma linha REAL do banco, entao a imagem carrega nome e
# whatsapp de cliente. Regenerar com:
#   python ferramentas/foto_fila.py fila 1280
ferramentas/fotos/

# MESMO motivo, para as fotos do ferramentas/foto.py, que grava em docs/design/
# e por isso escapava da regra acima. Medido em 11/08/2026: a foto_fila_1280.png
# mostra, legivel, nome e telefone completo de cliente real (o fixture do harness
# usa linha de verdade do banco). Repo privado nao desfaz historico de git.
# Regenerar quando precisar olhar:
#   python ferramentas/harness.py && python ferramentas/foto.py fila 1280
docs/design/foto_*.png
