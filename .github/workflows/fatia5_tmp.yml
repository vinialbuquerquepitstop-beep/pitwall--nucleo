name: fatia5-temp
on:
  push:
    branches: [codex/fila-operacional-v2-fatia5]
permissions:
  contents: write
jobs:
  fatia5:
    if: ${{ !contains(github.event.head_commit.message, '[fat5-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/fila-operacional-v2-fatia5
          fetch-depth: 0
      - name: Prepare deps
        run: python -m pip install esprima
      - name: Apply Fatia 5
        run: python ferramentas/patch_fila_operacional_v2_fatia5.py
      - name: Validate
        run: |
          git diff --check
          python ferramentas/validar.py
          node ferramentas/prova_fila_operacional_v2.js
          node ferramentas/prova_fila_operacional_v2_fatia3.js
          node ferramentas/prova_fila_operacional_v2_fatia4.js
          node ferramentas/prova_fila_operacional_v2_fatia5.js
      - name: Validate scope
        run: |
          git diff --name-only
          test "$(git diff --name-only | sort | tr '\n' ' ')" = "public/app.css public/app.js "
      - name: Commit product
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add public/app.js public/app.css
          git commit -m "feat(crm): aplica Modo Proximo da Fatia 5 [fat5-applied]"
          git push origin HEAD:codex/fila-operacional-v2-fatia5
